services:
  - type: web
    name: telegram-task-planner-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_render.py
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false